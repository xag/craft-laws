# The generality ledger: does the semantic twin express every law?

**THE OBJECTIVE — read this before the numbers.** The law set was grown to 57 to test ONE claim (ledger: `the-vocabulary-converges`): an app's semantic twin — its state machine, drawing, and companion vocabularies — can express ANY law or spec, and the vocabulary needed converges. There is no judge-forever category. The twin's authoring cost is accepted because the twin is empirically licensed: drawings by walks (drift), models by tapes (refine) — judgment moves to authoring time and is verified there, never trusted.

**THE QUESTION THIS FILE ANSWERS is model-expressibility — "can the twin state this law's falsifier?" — NOT "what is the cheapest way to check it."** An earlier version of this file classified by implementation cost (decider vs instrument vs compiler) and that framing misled: escape-closes-the-overlay sat in the "instrument" row for a day when it compiles against the existing twin with zero new vocabulary (per-overlay reachability — proved over chores' 816 states the day the question was actually asked). Implementation routes are recorded per law below, but the census is by what the MODEL can say.

**The convergence series** (new vocabulary each mechanization demanded — the falsifier `the-marginal-vocabulary-stops-shrinking` watches this line): base drawing ~6 ideas · composed-prose **+0** · plurals **+2** · one-surface-one-job **+1** · rare-action-folds-away **+1** · status-is-visible **+0** · one-act-one-name **+0** (later +1: generic_keys) · targets-are-thumb-sized **+0** · error-neither-begs-nor-blames **+0** · sentences-under-25 **+0** · escape-closes-the-overlay + no-keyboard-trap (model half) **+0** · glossary term kind **+1 kind** → glossary-first + no-calque (banned-variant halves) at once. Append every new crossing here.

## Status: the census of 57 by model-expressibility

**Running today (14):** empty-state-never-contradicts, composed-prose, plurals-and-agreement, one-surface-one-job, rare-action-folds-away, status-is-visible, one-act-one-name (compilers, épure prove) · a-way-back, escape-closes-the-overlay, no-keyboard-trap model-half (épure reach) · targets-are-thumb-sized (solver over measured premises) · error-neither-begs-nor-blames, sentences-stay-under-twenty-five-words (deciders) · glossary-first + no-calque mechanical halves (lexicon compilers over the term glossary, craft/lexicon.py).

**Expressible as-is or by construction — unpaid implementation, no new ideas (~14):** navigation-keeps-its-order (drawn element order), one-question-per-page (intents per surface), ellipsis-promises-more-input (action targets a sheet: derivable from the graph, +0 — an earlier estimate of +1 was wrong), every-input-labeled + no-placeholder-labels (vocabulary rule + generation), language-declared + base-direction (generated markup carries them by construction), string-id-tracks-meaning, no-cross-context-string-reuse, pseudolocale, locale-machinery/list-patterns/collation (repo-static over catalogue and code), no-system-vocabulary (banned termset in the lexicon), front-load's generic-heading half (decider).

**One shared domain fact away (~10):** `irreversible` on actions → check-before-commit, destructive-is-set-apart · `collects` on inputs → one-entity-one-field, known-date-three-boxes, keyboard-matches-the-field, no-autocorrect-on-identifiers · `optional` → mark-optional-and-required-alike · `asks` → never-ask-twice · `answer-shape` → field-width (with measured widths) · `verb` on actions → says-what-happens (against the glossary). These facts describe the DOMAIN, not the laws — the signature of a converging vocabulary.

**The rendered-world boundary — the model's existing discipline, not a failure (~12):** error rendering (an `erring` walk lane), colour-alone (grayscale diff), RTL screenshots, key events, touch phases, orientation, truncation marks, text-in-images. Measured premises + solved claims, exactly how targets and fits work today. The model often carries half the law (the exit exists: proved; Escape is its name: measured).

**Reading residue — shrinking, watched (~5):** the calque nobody pre-listed, tone/register beyond the voice lexicon, metaphor-lands, passives beyond a wordlist. The hypothesis bets the lexicon family reaches these too; each lexicon addition must shrink this bucket or the falsifier fires.

**Laws the model REFUSES in principle: none found.** The nearest candidates (focus mechanics, event phases) dissolve if focus becomes a state variable — an extension, not a defeat.

## The three invoices between 14 and ~52

1. **Shared domain facts** (~6 facts, listed above) — hours each, several laws per fact.
2. **The language substrate** — the glossary landed (interface@0.2.0 `term`: a concept with its settled word and banned variants per language); next the act-verb link and the voice lexicon. The app's LANGUAGE as data, the drawing's move repeated for words.
3. **Walker instruments** — DOM inspections and behavioural probes, written once in the walker's orbit, no vocabulary at all.
